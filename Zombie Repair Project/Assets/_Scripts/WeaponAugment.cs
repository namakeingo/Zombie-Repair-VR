using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Define behaviour of gun based on augmentation parameters
/// </summary>
public class WeaponAugment : MonoBehaviour
{
    public float fireRateMultiplier = 1;
    public int spread = 0;
    public int burstPower = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
